# Core runtime tested by CI (Python 3.12). Keep exact direct versions so a
# scheduled build cannot change behavior merely because PyPI published a release.
pandas==3.0.5
openpyxl==3.1.5
xlsxwriter==3.2.9
